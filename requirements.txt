# Production dependencies
Flask==2.3.2
pyharmonics==1.4.3
openai==1.60.2
python-dotenv==1.0.1
gunicorn==23.0.0  # 23.x 起不再依赖 pkg_resources（python:3.12+ 镜像无 setuptools，20.1 会启动失败）
pydantic==2.13.4
supabase==2.15.0
redis==5.0.8
upstash-redis==1.7.0
rq==1.16.2
socksio==1.0.0  # Required when httpx uses a SOCKS proxy (all_proxy)
pyyaml>=6.0  # For gate.yaml, loop-state YAML files
prometheus-client>=0.19.0  # For /metrics endpoint
tiktoken>=0.5.0            # For accurate memory token counting (memory-budget.md)

# Transitive dependencies of pyharmonics that must be constrained to avoid
# a websockets version conflict:
#   - yfinance>=0.2.59 requires websockets>=11 (uses websockets.sync/asyncio).
#   - yfinance>=1.0 requires websockets>=13.
#   - alpaca-trade-api declares websockets<11, but imports OK with 11.0.3 in practice.
#   - pyharmonics 1.4.3 uses TimeFrameUnit.Week/Month, which needs
#     alpaca-trade-api>=2.0.0 (3.x is the installable range on Python 3.11).
#   - supabase>=2.15.0 (via realtime) requires websockets>=11.
# Pin to 11.0.3 so supabase realtime imports successfully while keeping
# pyharmonics/alpaca functional.
alpaca-trade-api==3.2.0
curl-cffi==0.10.0
yfinance==0.2.57
websockets==11.0.3

# Development / test dependencies (install with: pip install -r requirements-dev.txt)
# pytest==9.1.1
# pytest-cov==7.1.0
