Flask==2.3.2
pyharmonics==1.4.3
openai==1.60.2
python-dotenv==1.0.1
gunicorn==20.1.0
pydantic==2.13.4
pytest==9.1.1
pytest-cov==7.1.0
supabase==2.15.0
redis==5.0.8
rq==1.16.2
socksio==1.0.0  # Required when httpx uses a SOCKS proxy (all_proxy)

# Transitive dependencies of pyharmonics that must be constrained to avoid
# a websockets version conflict:
#   - yfinance>=0.2.59 requires websockets>=11 (uses websockets.sync/asyncio).
#   - yfinance>=1.0 requires websockets>=13.
#   - alpaca-trade-api requires websockets<11.
#   - pyharmonics 1.4.3 uses TimeFrameUnit.Week/Month, which needs
#     alpaca-trade-api>=2.0.0 (3.x is the installable range on Python 3.11).
#   - supabase>=2.15.0 (via realtime) requires websockets>=11.
# Pin these so both packages can coexist. Note: supabase may upgrade
# websockets at install time; local dev has been observed to work, but
# this is a known tension that should be revisited for production.
alpaca-trade-api==3.2.0
yfinance==0.2.57
websockets==10.4
